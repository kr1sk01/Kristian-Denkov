@page "/admin"

@inherits ComponentBase

@rendermode InteractiveServer

@if (isAdmin)
{
    <h1>Adminceto BACE!</h1>
    <RadzenButton Click="@(() => NavigationManager.NavigateTo("/GameStatusMain"))" Text="Manage Game Statuses" ButtonStyle="ButtonStyle.Secondary" />
    <RadzenButton Click="@(() => NavigationManager.NavigateTo("/GameTypeMain"))" Text="Manage Game Types" ButtonStyle="ButtonStyle.Secondary" />
    <RadzenButton Click="@(() => NavigationManager.NavigateTo("/TeamTypeMain"))" Text="Manage Team Types" ButtonStyle="ButtonStyle.Secondary" />
    <RadzenButton Click="@(() => NavigationManager.NavigateTo("/ChampionshipTypesMain"))" Text="Manage Championship Types" ButtonStyle="ButtonStyle.Secondary" />
    <RadzenButton Click="@(() => NavigationManager.NavigateTo("/ChampionshipStatusMain"))" Text="Manage Championship Statuses" ButtonStyle="ButtonStyle.Secondary" />
}

@code {
    
}
