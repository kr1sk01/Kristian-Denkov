@page "/addgamestatus"
@using System.ComponentModel.DataAnnotations
@using System.Text.Json

@inject DialogService dialogService
@rendermode InteractiveServer


<center>
    <h3>Add Game Status</h3>
    <div style="justify-content:center;text-align:left;width:75%;">
        <RadzenTemplateForm Style="" TItem="GameStatus" Data=@model Submit=@OnSubmit InvalidSubmit=@OnInvalidSubmit>
            @if (showError)
            {
                <RadzenAlert Variant="@Variant.Filled" AlertStyle=@AlertStyle.Primary Size=@AlertSize.Small Shade="Shade.Lighter" AllowClose=false>
                    <RadzenText TagName="TagName.P">@errorMessage</RadzenText>
                </RadzenAlert>
            }
            <RadzenStack Gap="1rem" Class="rz-p-sm-12">
                <RadzenFormField Text="Game Status Name" Variant="@variant">
                    <ChildContent>
                        <RadzenTextBox Name="Name" @bind-Value=@model.Name />
                    </ChildContent>
                    <Helper>
                        <RadzenRequiredValidator Component="Name" Text="Game Status is required." />
                    </Helper>
                </RadzenFormField>
                <RadzenButton ButtonType="ButtonType.Submit" Text="Add Game Status"></RadzenButton>
            </RadzenStack>
        </RadzenTemplateForm>

    </div>
</center>

@code {

    Variant variant = Variant.Outlined;


    private GameStatus model = new GameStatus();
    private string? errorMessage;
    private bool showError = false;


    void OnSubmit(GameStatus model)
    {

        if (model.Name.Length <= 3)
        {
            showError = true;
            errorMessage = "Invalid Data!";
        }
        else
        {
            showError = false;
            errorMessage = "";
            //POST DATA
        }
    }
    void OnInvalidSubmit(FormInvalidSubmitEventArgs args)
    {

    }
}
