@page "/championshipDetails"
@using Microsoft.AspNetCore.WebUtilities
@inject HttpClient httpClient
@inject NavigationManager nManager
<PageTitle>Championship Info</PageTitle>

@if (championshipWithDetails != null)
{
    <div class="container">
        <div class="image-div">
            <img src="path/to/your/image.jpg" alt="Your Image">
        </div>
        <h1 class="heading">@championshipWithDetails.Name</h1>
        <div class="sub-divs">
            <div class="sub-div-left">@championshipWithDetails.</div>
            <div class="sub-div-right">Content of second sub-div</div>
        </div>
    </div>


}

@code {
    public string id;
    protected override void OnInitialized()
    {

        // Get the query parameters from the URL
        var uri = nManager.ToAbsoluteUri(nManager.Uri);
        var queryParams = QueryHelpers.ParseQuery(uri.Query);

        // Extract the data parameter from the query parameters
        if (queryParams.TryGetValue("id", out var data))
        {
            id = Uri.UnescapeDataString(data);

        }
    }

    private ChampionshipClassDto championshipWithDetails;

    protected override async Task OnInitializedAsync()
    {
        var test = await httpClient.GetFromJsonAsync<ChampionshipClassDto>($"api/ChampionshipClass/{id}");
        if (test != null)
            championshipWithDetails = test;
    }
}
#########################################################################CSS##########################################



.container {
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
}


.image-div {
    width: 200px;
    height: 200px;
    display: flex;
    justify-content: center;
    align-items: center;
}

    .image-div img {
        max-width: 100%;
        max-height: 100%;
    }

.heading {
    margin-top: 20px;
}

.sub-divs {
    display: flex;
}

.sub-div-left, .sub-div-right {
    flex: 1;
    padding: 10px;
    border: 1px solid #ccc;
}
.sub-div-left{
    background-color: #e50000;
    backdrop-filter:blur(10px);

}
.sub-div-right {
    background-color: blue;
    backdrop-filter: blur(10px);
}